# 意思疎通支援アプリ 設計案

対象: 脳卒中後の四肢麻痺・言語障害（気管切開・胃ろう・尿道カテーテル装着）
入力: 首振り／うなずき／視線移動／口の開閉（MediaPipeで検出）

---

## 1. 全体方針

- **3階層 × 4選択肢 = 64フレーズ**をメインの選択体系にする
- ただし「痛い」「苦しい」「ナースコール」など**緊急・高頻度の項目は階層の外**に常時表示（3クリックも待てない場面がある）
- 疲労時・精度が出ない時のために**スキャンモード（自動巡回選択）**を必ず用意する
- **位置は固定・並び替えしない**（頻度順の自動並び替えはしない）。運動障害のあるユーザーは「毎回同じ場所にある」ことによる筋肉記憶・予測操作が重要なため、通常のUXの定石（よく使うものを上に）とは逆に、一貫性を優先する

---

## 2. 操作方式（MediaPipe連携）

### ポインタ移動
- MediaPipe **Face Landmarker（Tasks Vision API）**の顔向き回転行列（facial transformation matrix）からyaw/pitchを取得
- 中央にニュートラル位置を設け、そこからの角度差を画面カーソル位置にマッピング
- 微小な震え対策として**指数移動平均（EMA）でスムージング**、中心付近に**不感帯（デッドゾーン）**を設定
- 感度（ゲイン）は可変にし、可動域が狭い患者でも端まで届くよう調整可能に

### クリック（決定）操作
- 口の開閉は MediaPipe の **blendshape「jawOpen」スコア**をそのまま閾値判定に使える（自前計算不要）
- 誤操作防止のため：
  - **開く閾値 > 閉じる閾値**（ヒステリシス）
  - 一定フレーム数（例: 150〜300ms）連続で閾値超えした場合のみ確定
  - 決定後は**クールダウン**（例: 800ms）を設け、連続誤爆を防止
  - 会話（発声できないので基本無音だが咳・嚥下動作等）による誤爆を避けるため、閾値はキャリブレーションで個人ごとに設定

### 補助ジェスチャー
- **首振り（yaw往復）＝「戻る」「キャンセル」**に固定割り当て
- **うなずき（pitch往復）＝「はい」の即答ショートカット**（階層をたどらず一発回答）
- 視線移動は、カメラ距離や麻痺姿位によって精度が安定しないことが多いので、**予備入力（カーソル微調整用）**として位置づけ、メインのポインタは頭部姿勢を主にする方が安定しやすい

### キャリブレーション（起動時に毎回実施）
1. 「まっすぐ前を見てください」→ ニュートラル姿勢を2秒計測
2. 「口を大きく開けてください」→ jawOpen最大値を計測し、個人別閾値を自動設定
3. 感度調整（カーソルが端まで届くか確認しながら微調整）

---

## 3. フェイルセーフ：スキャンモード

頭部制御が疲れてきた・精度が出ない場合の代替手段。

- カーソルを自分で動かす代わりに、**選択肢が一定間隔（可変速度）で自動的にハイライトを巡回**
- ユーザーは「口を開ける」の1アクションだけで選択（決定）
- 難易度・体力に応じて頭部操作モードとスキャンモードをワンタッチで切替可能にする

---

## 4. 常設の緊急/高頻度パネル（階層外）

画面上部などに固定表示、1クリックで即時発話・通知：

| ボタン | 内容 |
|---|---|
| 🔴 苦しい・緊急 | 大音量アラート＋ナースコール連携（可能なら） |
| 痛い | 即時TTS発話 |
| はい／いいえ | うなずき・首振りでも即答可（重複導線） |
| もう一度言って／わからない | 誤操作リカバリ用 |

---

## 5. 3階層メニュー（4×4×4 = 64フレーズ案）※ブラッシュアップ版

設計方針の変更点：
- はい／いいえは**首振り・うなずきの専用ジェスチャーで即答**できるため、階層メニューからは削除
- テレビは環境になし → 削除し、**DVD鑑賞・音楽鑑賞**を「そばにいてほしい」系カテゴリに統合
- 理解力低下を考慮し、**短く具体的な言葉**（イラスト化しやすい単語）に統一
- ネガティブ感情（つらい／悲しい／不安／怖い）を単独で並べる構成をやめ、**「気持ち＋してほしいこと」がセットになる表現**に再構成。感情を訴え続ける一方通行の構造ではなく、介助者が次の対応を取りやすい形にした

### A. 身体・体調
- **A1 痛み**: 痛い ／ どこが痛い（部位選択へ） ／ もっと痛い ／ 痛くなくなった
- **A2 かゆみ・不快感**: かゆい ／ 何か当たってる ／ 姿勢がつらい ／ むずむずする
- **A3 呼吸・気切**: 息苦しい ／ 痰がある（吸引して） ／ 気切が気になる ／ 呼吸は楽
- **A4 排泄・カテーテル**: カテーテル気になる ／ 違和感ある ／ お腹が張ってる ／ 便が出そう

### B. ケア・介助依頼
- **B1 体位・姿勢**: 体位をかえて ／ おこして ／ 横向きにして ／ 枕をなおして
- **B2 吸引・清拭**: 吸引して ／ 口をふいて ／ 体をふいて ／ 汗をふいて
- **B3 環境調整**: あつい ／ さむい ／ あかるくして ／ くらくして
- **B4 食事・胃ろう**: 胃ろうの時間？ ／ おなかすいた ／ 気持ち悪い ／ 栄養おわった

### C. 気持ち・してほしいこと
- **C1 ポジティブ**: うれしい ／ あんしんした ／ たのしい ／ ありがとう
- **C2 そばにいてほしい・癒し**: そばにいて ／ 手をにぎって ／ 音楽をきく ／ DVDをみる
- **C3 疲れ・休みたい**: つかれた ／ 少しやすみたい ／ しずかにして ／ ねむい
- **C4 意思確認**: わからない ／ もう一度言って ／ ちょっとまって ／ しつもんがある

### D. 会話・娯楽
- **D1 挨拶**: おはよう ／ こんにちは ／ おやすみ ／ おねがいします
- **D2 家族・面会**: 会いたい人がいる ／ 面会にきてほしい ／ 電話してほしい ／ 今はひとりにして
- **D3 時間・予定**: いま何時？ ／ 次の予定は？ ／ リハビリの話がしたい ／ 先生と話したい
- **D4 その他**: 本をよんでほしい ／ 外の様子が知りたい ／ 窓をあけてほしい ／ そのほか伝えたい（文字盤へ）

※ 内容は施設・家族と相談の上、患者ごとにJSON設定で編集できるようにするのが望ましい（本人の状態・生活歴で優先度が変わるため）。
※ イラスト制作の際は「動詞＋対象」がはっきりわかる構図（例：手を握る絵、DVDプレイヤーの絵、扇風機/毛布の絵）にすると誤認識が減る。

---

## 6. UI/フィードバック設計

- 1画面に4択タイル（2×2大きめ配置）、選択中はカーソルのホバーで拡大・色変化
- ホバー中に効果音、決定時に**Web Speech API（日本語音声）でTTS読み上げ**＋大きな文字表示（介助者が離れた場所からも見える）
- パンくずリスト表示（今どの階層にいるか）で迷子防止
- 「戻る」は常に画面内に固定表示 ＋ 首振りジェスチャーの両方で可能に

---

## 7. 技術スタック案

- **MediaPipe Tasks Vision (FaceLandmarker)** — ブラウザ内推論、CDN経由でロード可能
- フロント: React（またはシンプルなHTML/JS）
- TTS: Web Speech API（日本語音声）
- 設定: フレーズツリーはJSON化し、施設側で編集可能に
- ログ: 選択履歴を記録し、体調変化の把握や頻出要望の分析に活用（ただし表示順は固定のまま）

---

## 8. 今後の検討事項

- 部位選択（A1で「どこが痛いか」）用のボディマップ画面
- 文字盤モード（自由入力、五十音をスキャン選択）を最終救済手段として用意
- 介助者側の管理画面（フレーズ編集、ログ確認、感度調整）
- カメラ位置・照明条件による認識精度低下への対策（ベッド上での顔角度は限定されるため、実機での閾値再調整が必須）
